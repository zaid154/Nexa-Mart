{
  "name": "ecommerce-marketplace",
  "version": "1.0.0",
  "private": true,
  "description": "Electronics e-commerce marketplace (MERN)",
  "scripts": {
    "install:all": "npm install && npm install --prefix backend && npm install --prefix frontend",
    "dev": "concurrently -n server,client -c blue,green \"npm run dev --prefix backend\" \"npm run dev --prefix frontend\"",
    "seed": "npm run seed --prefix backend",
    "setup": "npm run install:all && npm run seed",
    "build": "npm run build --prefix frontend",
    "deploy": "firebase deploy",
    "deploy:hosting": "firebase deploy --only hosting",
    "deploy:functions": "firebase deploy --only functions"
  },
  "devDependencies": {
    "concurrently": "^9.1.0"
  }
}
