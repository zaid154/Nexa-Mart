# The frontend is what Vercel builds; everything else is dead weight on upload.
node_modules
**/node_modules

# The API deploys to Render, not here.
backend

# Build output is regenerated by Vercel. Shipping the local copy uploaded a
# byte-identical duplicate of frontend/public on every deploy.
frontend/dist

# Downloaded photo candidates awaiting review — never part of a deploy.
media-staging
media-report.json

*.log
.env
.env.*
!.env.production
